name: flutter_application_1
description: "Trashpot - Sistem Pengaduan Sampah Masyarakat"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  shared_preferences: ^2.2.3
  image_picker: ^1.0.7
  geolocator: ^12.0.0
  provider: ^6.1.2
  google_fonts: ^6.1.0
  url_launcher: ^6.2.5
  intl: ^0.19.0
  mime: ^1.0.5
  http_parser: ^4.0.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "android/asset/logoApk.jpg"

flutter:
  uses-material-design: true
